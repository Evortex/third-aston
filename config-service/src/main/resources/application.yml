server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Evortex/config-repo
          default-label: main
          clone-on-start: true

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/
